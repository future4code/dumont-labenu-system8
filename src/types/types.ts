export type student = {
    id: number,
    name: string,
    email: string,
    birth_date: string,
    mission_id: number
}

export type teacher = {
    id: number,
    name: string,
    email: string,
    birth_date: string,
    mission_id: number
}

export type studentId = {
    id: string
}

